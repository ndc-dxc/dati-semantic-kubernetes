---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: dati-semantic-admin
  namespace: ndc-test
  labels:
    application: dati-semantic-admin
spec:
  host: admin-ndc-test.apps.cloudpub.testedev.istat.it
  to:
    kind: Service
    name: dati-semantic-admin
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
